#文学贴吧
system -- 框架程序 -一般上线后不允许改动
--cache -- 模版编译缓存
--class -- 系统类文件
--func  -- 系统函数　及扩展函数（系统级一直被载入，非系统级可以后加载）
--library--库文件-其他扩展类（工具类）
--model -- 数据对象封装 一般比较复杂的数据操作可以在这里封装一层-用于增强数据处理
--template -- 原始模版(保护-不被apche直接访问)
--user——class -- 用户自定义类文件
-- config.inc.php  配置文件
-- router.class.php  路由类
-- controler 控制类
-- modules - 模块类　自动调用模块同名类　用于模块的初始化　基类不应该直接关联业务逻辑
	　　　此类的扩展即控制器类关联基本逻辑，控制器的扩展直接关联业务逻辑
approot---可以直接访问的内容
source -- 扩展程序-例如 fcedtor之类
static -- 静态页面 图片 css 等
TEMP   -- 缓存-备静态缓存(html)
upload -- 上传文件目录
index.php  -- 唯一入口

#-------------------------------------------------------------------------#

